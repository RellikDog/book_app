<% books.forEach(ele => { %>
    <li>
        <img src=<%= ele.image %>>
        <h3><%= ele.title %></h3>
        <h5><%= ele.author %></h5>
        <p><%= ele.descript %></p>

        <button>Save to Bookshelf</button>
    </li>
    
<% }); %>